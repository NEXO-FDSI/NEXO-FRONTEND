# URL de NEXO-BACKEND. Vite la incrusta en el build: cambiarla exige recompilar.
# El origen del frontend (p. ej. http://localhost:5173) debe estar en CORS_ORIGINS del backend.
VITE_API_URL=http://localhost:8000
